workflows:
  flutter_tech_talk:
    name: Flutter CI TechTalk
    max_build_duration: 15
    environment:
      flutter: stable
    scripts:
      - name: Analisando código com dart analyze
        script: dart analyze
      - name: Rodando testes unitários (se tiver)
        script: flutter test
